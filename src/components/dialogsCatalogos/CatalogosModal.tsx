import { Dialog } from "@mui/material";
import React from "react";

export const CatalogosModal = () => {
  return (
    <Dialog
      open={false}
      // fullWidth
    >
      <div>CatalogosModal</div>
    </Dialog>
  );
};
